# 세션 2026-06-24-29 (수거 게이미피케이션 Phase 1 + systemd 영구구동 + 미완료 목록)

## 완료된 작업
- [x] 지도 핀 팝업 폭 확대 + '게시글 보기' 링크 모던화(밑줄 제거/pill) + 상태 배지 시각화 (`259fc44`)
- [x] 개인 수거 인증 루프(사진 인증→완료+50XP, 관리자 사후검수 철회) — SNS `/reports/{id}/cleanup`, `/admin/cleanups`, GIS pins report_id (`259fc44`)
- [x] **systemd 영구 구동 안정화** — 9개 서비스(gateway/sns/game/gis/ai/data/mcp/ws/policy)를 `/home/spark/.config/systemd/user/metaflow-*.service` + `metaflow.target`로 등록. linger 활성, Restart=always, 자동 재시작 검증(gis kill→3초 복구). 더 이상 세션 종속으로 죽지 않음.
  - 관리: `systemctl --user {status|restart|stop} metaflow-<svc>`, 전체 `metaflow.target`

## ⚠️ 데이터 무결성 메모 (오해 해소)
- 세션 중 "DB가 지워졌다" 오해 발생 → 실제로는 **백엔드 서비스 다운**이 원인(데이터 온전: users 109/trash_reports 1097/posts 1345). 승인된 aihub 더미 119건 외 삭제 없음. systemd 전환으로 재발 방지.

## 미완료 작업 (우선순위 순)

### P0 — Phase 2 (수거 퀘스트) 마무리 [구현 중단됨, 미커밋]
- 백엔드 코드 작성됨(미커밋): `scripts/migrate_cleanup_quests.py`(DB 적용 완료) + `services/game/main.py`(자동생성 `_generate_cleanup_quests`/스케줄러/`/cleanup-quests`·`/cleanup-quests/{id}`·`/cleanup-quests/generate`) + `services/sns/main.py`(수거 시 quest_target_reports 진행+시간보너스 `cleanup_quest_bonus`+완료 판정)
  - 라우트 충돌(`/quests/cleanup`↔`/quests/{id}`) → `/cleanup-quests`로 변경 직후 중단
- [ ] **백엔드 재검증**: 대상 제보 수거→진행도·시간보너스·완료 루프 (생성 1건은 검증됨)
- [ ] **프론트 미구현**: 탐색 탭 수거 퀘스트 카드 + 마감 카운트다운 + 진행도 바 + '치우러 가기' 버튼(지도 연동)
- [ ] **Phase 2 커밋**

### P1 — 테스트 데이터 정리
- [ ] `image_urls=ARRAY['test://q']` 테스트 제보 6건 + 자동 수거퀘스트 1건 + quest_target_reports 6건 제거 (실데이터 무관, 제 검증 흔적)

### P1 — 버전/릴리스
- [ ] **v0.1.0 태그 미생성**(README는 v0.1.0인데 git tag 없음) + Phase1 수거(259fc44)까지 묶어 태깅 + `git push origin main --tags`
- [ ] CLAUDE.md 5절 서비스 관리 명령을 **systemd 방식으로 갱신** (현재 `conda run &` 기술)

### P2 — NAS 2·3단계 (session-27, 별개 트랙)
- [ ] `vm.max_map_count=262144`(DSM Task Scheduler), ES 포트 정합, DOCKER.md Synology 절, 실 기동 검증

### P2 — 판단 보류 (버그 아님)
- [ ] admin '내 퀘스트'는 멤버십 기반(정상) → 필요 시 admin '전체 퀘스트 보기' 추가

## 신규 산출물
- `docs/industry/산학협력-활용방안.md` — 플랫폼 기반 산학협력 아이디어 (이번 세션 작성)

## 수정/생성 파일 (미커밋)
- `platform/services/game/main.py`, `platform/services/sns/main.py`, `platform/scripts/migrate_cleanup_quests.py`
- `~/.config/systemd/user/metaflow-*.service`, `metaflow.target` (repo 외부)
