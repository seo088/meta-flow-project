# 세션 2026-07-22

## 완료된 작업
- [x] 166번 `hong` 정적 프리뷰(`http://203.234.62.166:8600`)에서 프로필의 API 콘솔 버튼이 `:8500/api-console`으로 이동하는 원인 확인
- [x] `platform/apps/web/index.html`의 API 콘솔 링크를 `API_CONSOLE_URL`로 분리
- [x] 프리뷰 포트 `8600`에서는 `location.origin + '/api-console.html'`로 이동하고, 운영 환경에서는 기존 `GW + '/api-console'`을 유지하도록 수정
- [x] 프로필의 데이터 제공 API 문서 표에 신규 요청 파라미터(`trash_size`, `has_image`, `has_location`, `include_items`)를 반영
- [x] 마이페이지 타임라인 하단의 데이터 활용 API 신청/문서 블록 제거
- [x] 데이터 활용 API 신청/문서 블록을 메시지함 탭 내부에서만 로드·표시하도록 수정

## 미완료 작업 (우선순위 순)
### P0 — 즉시 필요
- 176번 운영 서버에 API 고도화 코드와 프론트 문서 변경사항 배포 여부 결정

### P1 — 기능 보완
- 166번 `hong` 계정에서 실제 API까지 실행하려면 FastAPI 의존성 설치 후 data/gateway 서비스를 별도 포트로 구동

### P2 — 품질 개선
- 프리뷰/운영 API Base URL을 환경 표시 배너로 노출해 사용자가 현재 어느 서버를 보고 있는지 구분 가능하게 개선

## 발견된 버그/이슈
- `index.html`의 API 콘솔 버튼이 `GW` 상수(`http://현재호스트:8500`)를 직접 사용해 8600 프리뷰에서도 8500 운영 콘솔로 이동했다.
- 데이터 활용 API 신청/문서 블록이 프로필 탭 공통 하단에 있어 타임라인 화면에도 노출됐다.

## DB 스키마 변경
- 없음

## 수정된 파일 목록
- `platform/apps/web/index.html`
- `CHANGELOG.md`
- `docs/sessions/2026-07-22-session.md`

## 주의사항
- 166번 프리뷰는 UI 확인용 정적 서버이며, API 실행까지 검증하려면 별도 gateway/data 런타임이 필요하다.
