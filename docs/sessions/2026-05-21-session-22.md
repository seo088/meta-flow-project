# 세션 2026-05-21-22 (UI/UX 버그 + 개선)

## 완료된 작업

### 버그 수정
- [x] **버그1: 타임라인 댓글 미열림 (ID 충돌)**
  - 원인: 같은 게시물이 피드/타임라인/플레이그라운드에 동시 렌더링되면 `id="comments-${p.id}"` ID 충돌. `getElementById`는 첫 매칭만 반환 → 보이지 않는 영역만 토글
  - 수정: `toggleComments(postId, ev)` 시그너처. `ev.target.closest('.feed-card')` → 같은 카드의 `.comments-section` 토글. 폴백: `.panel.active` 안에서 검색
  - `addComment`, `deleteComment`도 `_findPostElement` 헬퍼 + `querySelectorAll`로 모든 인스턴스 갱신
  - 파일: `apps/web/index.html` (feedCard 호출처 + toggleComments + addComment + deleteComment)

- [x] **버그2: 알림 뱃지 안 사라짐**
  - 원인: `markAllNotifsRead`, `dismissNotif`, `acceptQuestInvite`, `navigateToNotif`가 읽음 처리 후 `updateNavBadges()` 미호출
  - 수정: 위 4개 함수 끝에 `updateNavBadges()` 호출 추가. `navigateToNotif(postId, notifId)` 시그너처로 자동 읽음 처리
  - 알림 뱃지(nav-notif-badge) 클릭 진입점을 `showProfile` → `showNotificationsTab()`로 변경

- [x] **버그3: 글쓰기 컨텍스트 유지 (퀘스트 글쓰기 잔존)**
  - 원인: `smartCompose()`가 `quest-detail-view.style.display`만 체크. 탭 이동 시에도 quest-detail-view는 block 유지 + currentQuestId 살아있어 다른 탭에서도 퀘스트 모드 진입
  - 수정: `document.querySelector('.panel.active')?.id==='p-quests'` 추가 체크. 동시에 글쓰기 통합(개선5)으로 자연스럽게 해결 — `_questComposeId` 기반 → `_composeTarget` 기반으로 일원화

### 개선
- [x] **개선4: 알림 목록 페이지** (`p-notifications` 패널)
  - 신규 패널 `<div class="panel" id="p-notifications">` 추가
  - `loadNotificationsPage()` — `/notifications?size=50` 호출. 필터: 전체/안 읽음
  - 알림 타입별 렌더링:
    - `comment`/`mention` → 클릭 시 해당 게시글로 이동 + 자동 읽음
    - `quest_invite` → 참여하기 카드 (기존 acceptQuestInvite 재활용)
  - 안 읽음 강조 (warm-glow 배경 + accent 보더 + 우측 점)
  - 상단 컨트롤: 필터 토글, 모두 읽음
  - `showTab('notifications')` 핸들러 등록
  - 네비 알림 뱃지 클릭으로 진입

- [x] **개선5: 글쓰기 통합 + 퀘스트 선택**
  - UI:
    - 컴포즈 모달에 "📝 플로그 / 🎯 퀘스트" 게시 위치 세그먼트
    - 퀘스트 선택 시 → 참여 중인 퀘스트 드롭다운(`/quests/my-quests`)
    - 카테고리 바(일반/제보)는 플로그 모드일 때만 표시
    - 공개 옵션이 위치별로 동적 변경
  - JS 흐름:
    - `openCompose(opts)` 통합. `opts.questId` 인자로 퀘스트 자동 선택 가능
    - `openQuestCompose(questId)` → `openCompose({questId})` 위임 (역호환)
    - `setComposeTarget(target, preselectQuestId)` — 위치 전환 핸들러
    - `loadComposeQuestOptions(preselectId)` — 참여 퀘스트 로드
    - `submitPost()` — `_composeTarget==='quest'`일 때 select에서 questId 가져와 `/quests/{id}/feed`로 분기, 그 외는 기존 SNS 분기
    - `smartCompose()`는 퀘스트 상세 화면(p-quests + feed탭)에서 호출 시 questId 자동 선택

## 미완료 작업

### P2 — 이월
1. XP 미구현 항목: boss_raid_joined(+100), first_zone_report(+100), like_received(+2)
2. PWA 설정 (HTTPS 전환 후)
3. 모바일 피드 무한 스크롤 (스크롤 위치 유지)
4. 기존 `[ZONE_FIX]`/`[QUEST_INVITE]` DM cleanup 스크립트
5. Gemini 응답 캐싱 (Redis)

## 수정된 파일
- `platform/apps/web/index.html` — 전부 (단일 파일 SPA)
  - `feedCard()`: `toggleComments` 호출에 event 전달
  - `toggleComments`: closest-feed-card + visible-panel 폴백
  - `addComment`/`deleteComment`: 모든 인스턴스 갱신 (querySelectorAll)
  - 알림 함수들: `updateNavBadges()` 호출 추가
  - `nav-notif-badge` onclick → `showNotificationsTab()`
  - 신규 패널 `p-notifications` + `loadNotificationsPage`, `renderNotifPageItem`, `setNotifFilter`, `markNotifReadInPage`
  - 컴포즈 모달: 게시 위치 세그먼트 + 퀘스트 picker
  - `openCompose(opts)` 통합, `openQuestCompose` 위임
  - `setComposeTarget`, `loadComposeQuestOptions`, `loadQuestSuggestTagsForCompose` 신규
  - `submitPost`: `_composeTarget` 기반 분기
  - `closeCompose`: 위치 세그먼트/picker 리셋

## 백엔드 변경
- 없음 (기존 API 활용)
- 사용 API: `/quests/my-quests` (Game), `/notifications`, `/notifications/read`, `/notifications/system`, `/posts/create`, `/posts/report`, `/quests/{id}/feed`

## 주의사항
- 컴포즈 모달의 `_questComposeId` 변수는 역호환을 위해 유지되지만 새 흐름에서는 사용 안 함 (모두 `_composeTarget` + `compose-quest-select`로 일원화)
- 알림 목록 페이지는 `goBack()`이 처리되므로 다른 탭에서 진입 후 뒤로가기 정상 동작
- 게이트웨이는 main 디렉토리에서 실행 중 (worktree 동기화 불필요)
