# 세션 2026-06-26-25

## 완료된 작업
- [x] 최신 세션 파일과 최근 Git 상태 확인
- [x] 로컬 프로젝트 폴더에 Git 저장소 초기화
- [x] `hong@203.234.62.166:4441` SSH 접속 확인
- [x] 서버 기본 상태 확인: 호스트명 `spark-hub`, 업타임 11일, 루트 디스크 915G 중 33% 사용, 사용 가능 메모리 약 52Gi
- [x] 서버 포트 상태 확인: `8500~8505`, `8510`, `5433`, `6380`, `9001`, `9093` 모두 미사용
- [x] 기존 서비스 확인: Docker 기반 `aiready-*`, Wiki.js, Fuseki, GraphDB, Redis, PostgreSQL 등이 `80`, `3001`, `3030`, `5432`, `5500`, `5600`, `6379`, `7200`, `8002` 사용 중
- [x] 배포 사용자 권한 확인: `/home/hong` 쓰기 가능, Docker 접근 가능, `rsync`/`tar`/`unzip` 사용 가능
- [x] 대용량 `data/` 원천 데이터가 Git 커밋에 포함되지 않도록 `.gitignore` 보강
- [x] GitLab 원격 저장소 `ssh://git@203.234.62.175:8022/system/meta-flow.git`를 `origin`으로 등록
- [x] 로컬 `young` 브랜치 생성 및 `origin/young` 추적 설정 추가
- [x] 서버 `/home/hong/meta-flow` 경로 업로드 준비
- [x] Git bundle 방식으로 서버 `/home/hong/meta-flow`에 코드 업로드 완료
- [x] 서버 저장소의 현재 브랜치가 `young`이고 `origin`이 GitLab URL인지 확인
- [x] 서버 `hong` 계정에 GitLab 등록용 SSH 키 생성
- [x] 서버 `/home/hong/meta-flow`에 `young` 브랜치 bundle 재전송 및 파일 수 확인
- [x] `Calculator test` 폴더에 덧셈/곱셈 계산기 정적 페이지 추가
- [x] 병합 편의를 위해 연산 정의를 `operations.js`의 `OPERATIONS` 테이블로 분리
- [x] Node 테스트와 Chrome 파일 열기 검증으로 계산기 동작 확인

## 미완료 작업 (우선순위 순)
### P0 — 즉시 필요
- 없음

### P1 — 기능 보완
- 실제 배포 시 대상 경로를 `/home/hong/meta-flow-young` 등으로 확정하고 `.env` 파일을 별도로 준비해야 함
- Python 서비스를 직접 실행하려면 `conda`가 PATH에 없으므로 venv 또는 Docker 기반 실행 방식을 결정해야 함
- GitLab SSH 인증이 현재 PC 키 기준 `Permission denied (publickey)`로 실패하므로 GitLab에 공개키 등록 또는 올바른 SSH 키 설정이 필요함
- 서버 작업 저장소에서 직접 push/pull하려면 `/home/hong/.ssh`에 GitLab용 키를 준비하고 GitLab 계정 또는 Deploy Key에 공개키를 등록해야 함
- 생성된 서버 공개키: `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICSxRM3jm1BcT7iGL5r6UJKCLHBPFgBrdoih+NqyHypO hong@spark-hub meta-flow`

### P2 — 품질 개선
- 외부 접근을 80/443으로 붙일 경우 기존 Docker nginx 구성을 확인하고 충돌 없이 프록시 설정을 조정해야 함

## 발견된 버그/이슈
- 22번 SSH 포트는 닫혀 있으며 실제 접속 포트는 로컬 SSH 설정 기준 `4441`
- 80번 포트는 기존 Docker nginx가 사용 중이며 루트 요청은 `{"detail":"Not Found"}` 404 반환
- `hong` 계정은 sudo 그룹에는 있으나 비밀번호 없는 sudo는 불가
- `/home/spark/research/meta-flow` 경로는 `hong` 계정에서 접근 불가
- `/home/hong`, `/home/gyul` 아래에는 기존 `meta-flow` 관련 디렉터리가 없음
- GitLab 원격 저장소 접근 테스트에서 공개키 인증 실패 발생
- 서버 `hong` 계정에도 GitLab용 개인키/공개키 파일이 없어 `git@203.234.62.175:8022` 인증은 아직 실패함
- 서버 공개키 생성 후에도 GitLab에 공개키를 등록하기 전까지는 `Permission denied (publickey)`가 계속 발생함
- `ssh 203.234.62.166`처럼 사용자/포트를 생략하면 다른 로컬 SSH 설정과 충돌할 수 있으므로 `ssh -p 4441 hong@203.234.62.166`를 명시해야 함

## DB 스키마 변경
- 없음

## 수정된 파일 목록
- `.gitignore`
- `CHANGELOG.md`
- `Calculator test/index.html`
- `Calculator test/styles.css`
- `Calculator test/operations.js`
- `Calculator test/app.js`
- `Calculator test/operations.test.cjs`
- `Calculator test/browser-compat.test.cjs`
- `docs/sessions/2026-06-26-session-25.md`
- `.git/config` (커밋 대상 아님: `origin`, `branch.young.*` 설정)

## 주의사항
- 서버 업로드 시 `data/`, `.env`, `node_modules/`, 캐시 디렉터리는 제외해야 함
- 메타플로우 기본 앱 포트는 현재 비어 있어 사용자 프로세스 또는 Docker 컨테이너로 기동 가능
- 기존 서버에는 다른 Docker 서비스가 운영 중이므로 80/443 프록시 변경은 사전 확인 후 진행해야 함
- `Calculator test`는 모듈 스크립트를 쓰지 않고 일반 스크립트로 구성해야 `index.html`을 파일로 직접 열어도 동작함
