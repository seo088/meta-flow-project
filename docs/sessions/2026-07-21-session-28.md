# 세션 2026-07-21-28

## 완료된 작업
- [x] 최신 세션 문서, 최근 커밋, 현재 브랜치와 작업 트리 상태 확인
- [x] 작업 브랜치 `feat/code-sync-main` 생성
- [x] `C:/Users/jwon/Downloads/meta-flow-main/meta-flow-main` 원본과 현재 작업 트리 파일 목록 비교
- [x] 공통 파일 중 실제 차이는 `GITLAB_TUTORIAL.md` 1개뿐임을 확인
- [x] 원본 전용 파일은 `platform/apps/web/avatars/fc999475-cb8a-4828-bc27-20ccd9615d71.png` 1개뿐이며 `.gitignore`의 런타임 업로드 아바타 경로에 해당함을 확인
- [x] `platform/apps/web/index.html`, `platform/apps/web/api-console.html`, `platform/services/data/main.py`, `platform/services/gateway/main.py`, `platform/services/sns/main.py`, `platform/shared/core/ingest.py`, `platform/shared/core/trash_taxonomy.py`가 원본과 동일함을 확인
- [x] 원본 `GITLAB_TUTORIAL.md`에는 실제 GitLab PAT 형태 문자열이 남아 있어 현재 저장소의 placeholder 치환 버전을 보존하기로 결정

## 미완료 작업 (우선순위 순)
### P0 — 즉시 필요
- 없음

### P1 — 기능 보완
- GitLab `origin` SSH 키 등록 후 원격 `main`을 직접 fetch하여 로컬 폴더 기준이 아닌 원격 기준 동기화 여부 재확인

### P2 — 품질 개선
- 런타임 아바타 파일을 배포/백업 대상으로 관리할지 별도 운영 정책 결정

## 발견된 버그/이슈
- GitLab 원격 `ssh://git@203.234.62.175:8022/system/meta-flow.git`는 현재 로컬 SSH 키 인증이 거절되어 직접 fetch 불가
- `meta-flow-main`의 `GITLAB_TUTORIAL.md`에는 GitHub Push Protection이 차단할 수 있는 실제 토큰 형태 문자열이 포함되어 있음

## DB 스키마 변경
- 없음

## 수정된 파일 목록
- `CHANGELOG.md`
- `docs/sessions/2026-07-21-session-28.md`

## 주의사항
- 코드 파일은 `meta-flow-main`과 이미 동기화되어 있어 덮어쓰기 작업을 수행하지 않았다.
- `GITLAB_TUTORIAL.md`는 보안상 현재 placeholder 치환본을 유지한다.
- `platform/apps/web/avatars/`는 `.gitignore` 대상인 사용자 업로드/런타임 파일 경로이므로 코드 동기화 대상에서 제외한다.
