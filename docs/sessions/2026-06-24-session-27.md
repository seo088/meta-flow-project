# 세션 2026-06-24-27 (Synology NAS Docker 배포 — 1단계 완결 + 잔여 목록)

## 배경
RS3621xs+ (Xeon D-1541 x86_64 / 16GB ECC, 최대 64GB / 83.7TB)에 동일 서비스를 Docker로 운용 목표.
하드웨어는 풀스택(Kafka+ES 포함) 여유 → 차단 요인은 **컨테이너 구성 미완성**뿐.

### 근본 원인
원래 베어메탈 단일호스트에서 모든 서비스를 `127.0.0.1`로 띄우던 구조. 컨테이너화가 9개 서비스 중
6개(gateway/sns/game/gis/ai/mcp)까지만 배선되어, 나머지 실서비스와 gateway 프록시가 미완성이었음.

## 완료된 작업 (1단계 — 차단급) — 커밋 `5049efe`
- [x] **① compose `data`(8505) 추가** — app-base 상속, PostgreSQL 필수 + ES graceful 폴백, ES_HOST/PORT 명시
- [x] **② compose `ws`(8520) 추가** — Redis pub/sub WebSocket
- [x] **③ compose `policy`(8541) 추가** — 단독 서비스
- [x] **④ gateway 프록시 env화** — `services/gateway/main.py` ws/policy를 WS_SERVICE_URL/POLICY_SERVICE_URL 오버라이드 가능하게 (127.0.0.1 → 컨테이너 서비스명). gateway env에 두 URL 주입
- [x] **MCP 변수명 불일치 수정** — 코드(MCP_SERVER_URL) ↔ compose(MCP_SERVICE_URL) → MCP_SERVER_URL로 통일 (MCP 라우트도 컨테이너에서 깨지던 것)
- [x] **⑤ MINIO_PUBLIC_URL** — `.env.docker.example`에 NAS 실주소 필수 경고 명시 (배선은 이미 됨, 값은 배포시 .env에서 설정)
- [x] 검증: gateway py_compile OK, `docker compose config` 통과(서비스 15개, data/ws/policy 포함 확인)

## 미완료 작업 (우선순위 순)

### P1 — 2단계: NAS 환경 대응
- [ ] **⑥ Elasticsearch `vm.max_map_count=262144`** — ES 사용 시 필요. DSM Task Scheduler 부팅 트리거 스크립트(`sysctl -w vm.max_map_count=262144`)로 설정. 미설정 시 ES 컨테이너 기동 실패
- [ ] **⑦ ES 포트 정합성 정리** — `.env.docker.example`/코드 기본값(9201) vs compose 내부(9200) 혼재. 호스트 9201 / 컨테이너 9200으로 표기·주석 통일 (현재 data 서비스엔 ES_PORT:9200 명시해둠 → 코드 기본 9201은 컨테이너 밖 직접 실행시만 영향)

### P2 — 3단계: 정리/문서
- [ ] **⑧ 죽은 에이전트 라우트 정리** — gateway SERVICE_MAP의 `drone/data-agent/mobility/quest`(8531~8540)는 services/ 디렉토리 없음(미구현). compose와 무관하게 원래 죽은 라우트 → 맵에서 제거 또는 주석 처리해 혼란 방지
- [ ] **⑨ DOCKER.md Synology 절 추가** — Container Manager Project import 절차, .env 작성(특히 MINIO_PUBLIC_URL=NAS IP), 포트 매핑 표(5433/6380/8500~8541/9001~9002/9093/9201), "외부 빌드→레지스트리 push→NAS pull" 권장 흐름, vm.max_map_count 설정법
- [ ] **버전 v0.0.13 태깅** — NAS 대응 완료 시 (README 변경이력 + 태그)
- [ ] Docker compose **실 기동 검증** 미수행 — RS3621xs+ 또는 로컬에서 `docker compose up -d --build` 후 헬스체크 확인 필요

## 수정된 파일
- `platform/docker-compose.yml` (data/ws/policy 서비스 + gateway env)
- `platform/services/gateway/main.py` (ws/policy 프록시 env화)
- `platform/.env.docker.example` (MINIO_PUBLIC_URL NAS 경고)

## 주의사항
- 에이전트 서비스(drone 등) 4종은 코드 미구현 상태 — NAS 배포 범위 아님
- compose `data` 서비스는 ES 없이도 동작(PG 폴백)하므로 `profiles:search`에 넣지 않고 항상 기동하도록 둠
- 실 기동 검증 전까지 "구성 정합성 통과" 수준 (config validate만 완료)
