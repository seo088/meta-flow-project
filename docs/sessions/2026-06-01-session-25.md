# 세션 2026-06-01-25 (외부 DI 데이터셋 배치 적재 + 다축 분류체계)

## 완료된 작업

### 1. 댓글 멘션 자동완성 — 작성자 누락 수정 (견고화)
- `initMentionFocus` 포커스 시 드롭다운 미오픈 버그 → `handleMentionInput` 호출
- DOM 역파싱 대신 `feedCard` 렌더 시 `_postAuthors` 시드 (SSOT), `toggleComments`/`showMentionDropdown` 폴백

### 2. 외부 DI 데이터셋(메타플로깅_DI_데이터(260512).zip) 배치 적재
- zip: `메타플로깅_DI_데이터/image/*.jpg` 1044장 + `merged_records.json`(정답 라벨+좌표) + `plogging_labeling_tool.py`
- 추출: `data/03.External_data/extracted/`
- **적재 결과: 1035건**(신규 1025 + 기존 10), 위치 보유 1033(99.8%), 실패 0, 중복 9건 SHA-256 멱등 skip
- batch-id: `2026-06-01-di-full`, `2026-06-01-di-verify10`

### 3. 다축 분류체계 (SSOT: `shared/core/trash_taxonomy.py`)
- 품목 9종: general/plastic/large/food/unknown(빌트인) + cigarette/paper/vinyl/metal/glass/styrofoam(신규)
- 크기(size) · 수거구분(handling: normal/recycle/bulk) · 긴급성(severity: size+handling 자동산출)
- 한글라벨→key 매핑, 다중라벨→대표1개+gt_label.items 전체 보존
- 분포: cigarette 332·general 285·plastic 121·vinyl 121·paper 107·metal 31·styrofoam 18·large 15·glass 5

### 4. 적재 인프라 (내부 API SSOT)
- `shared/core/ingest.py` — 적재 코어(EXIF/멱등/MinIO/INSERT/zone매칭/gt_label)
- SNS `/admin/reports/ingest`(단건 multipart/url), `/admin/reports/ingest-batch`(배치 JSON, local_path/url)
- `scripts/batch_report_ingest.py` — API 클라이언트(dataset_bot JWT, jsonl 감사로그)
- `scripts/migrate_ingest_taxonomy.py` — DB 마이그레이션
- `.claude/skills/batch-report-ingest/SKILL.md`

### 5. 정책 연동 (사용자 결정 반영)
- **피드 전용 탭**: `/feed?source=external_di` 신설, 메인 피드는 external_di 제외 → 1044건 피드 범람 방지
- **XP/트렌드 미반영**: 배치 코어는 grant_xp/트렌드 캐시 미호출 (dataset_bot 격리, 제보 통계에만 반영)

### 6. UI
- 제보 작성 폼 카테고리 10종 + `/report-categories`(사전 SSOT) 엔드포인트
- 현황 카테고리 동적 렌더(typeOrder 하드코딩 제거) + 썸네일(GIS pins에 image_urls 추가)
- 관리자 "데이터셋" 서브탭: 배치 진행/건수/카테고리분포 + 전용 피드 보기
- 도움말 "쓰레기 분류 사전" 섹션

## DB 스키마 변경
- `trash_reports.gt_label jsonb`, `trash_reports.handling varchar` 추가
- `report_categories` 테이블 신설 + 11종 시드
- `users`: `dataset_bot`(role=system) 생성 (id=f2d472f8-a488-4a2d-8235-e31ef20c9623)

## 수정된 파일
- `platform/shared/core/trash_taxonomy.py` (신규), `platform/shared/core/ingest.py` (신규)
- `platform/services/sns/main.py` (ingest 라우트, /report-categories, /admin/reports/batches, feed source 필터)
- `platform/services/gis/main.py` (/pins/recent·/pins/nearby image_urls + 실제 trash_type)
- `platform/apps/web/index.html` (멘션 수정, 카테고리 매핑, 현황 동적화, 관리자 데이터셋 탭, 분류 사전)
- `platform/scripts/{batch_report_ingest,migrate_ingest_taxonomy}.py` (신규)
- `.claude/skills/batch-report-ingest/SKILL.md` (신규)

## 미완료 작업
### P1
- ADR-003(외부 데이터셋 적재) 문서, `.claude/agents/dataset-ingest.md` 에이전트 정의
- 버전 v0.0.11 태깅 (신규 API+DB스키마+UI → 버전업 대상)
### P2
- geofence_zones 가 데이터 좌표(lon 126.678~685/lat 35.94~35.95)를 포함 못함 → zone 매칭 0. 폴리곤 확장 검토
- 사용자 결정 보류: 카카오톡 EXIF 제거로 위치는 JSON 좌표 사용(정상)

## 주의사항
- 프론트(index.html)는 게이트웨이 no-cache 정적 서빙 → `Ctrl+Shift+R` 로 반영
- 배치 재실행은 SHA-256 멱등 → 중복 자동 skip (안전)
- 적재 롤백: `DELETE FROM trash_reports WHERE source='external_di'` (+ 연결 posts)
