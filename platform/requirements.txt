# Meta SW 플로깅 — 백엔드 서비스 슬림 의존성
# AI 서비스는 dummy 모드로 동작하므로 torch/cv2/ultralytics 제외 (선택적으로 requirements-ai.txt 사용)

# === 웹 프레임워크 ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9      # FastAPI 파일 업로드

# === 인증/JWT ===
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4

# === DB / 캐시 / 메시지 ===
asyncpg>=0.29.0
redis[hiredis]>=5.0.0
confluent-kafka>=2.3.0
elasticsearch[async]>=8.13.0

# === 검증/HTTP ===
pydantic>=2.7.0
pydantic-settings>=2.0.0
httpx>=0.27.0

# === 객체 스토리지 ===
minio>=7.2.0

# === 스케줄러 ===
apscheduler>=3.10.0

# === MCP ===
fastmcp>=2.0.0
mcp>=1.0.0

# === 이미지/EXIF 처리 (백엔드 GPS 추출) ===
Pillow>=10.0.0                # JPEG/PNG EXIF
pillow-heif>=0.16.0           # iOS HEIC 지원

# === 유틸 ===
faker>=25.0.0
rich>=13.7.0
python-dotenv>=1.0.0          # .env 로드 (옵션)
